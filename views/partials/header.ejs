<!DOCTYPE html>
<html lang="en">
<head>
  <title>BookNest</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    /* Custom CSS Variables for Branding */
    :root {
      --booknest-primary: #1e81b0; /* Deep Teal/Blue */
      --booknest-secondary: #004d73; /* Darker Blue for background */
      --booknest-accent: #ffc107; /* Warning yellow/gold */
    }

    body {
      /* REMOVED: text-transform: capitalize; for better title display */
      font-family: 'Inter', sans-serif;
    }

    /* --- Navbar Styling --- */
    .navbar {
      background-color: var(--booknest-secondary) !important;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding-top: 0.6rem;
      padding-bottom: 0.6rem;
    }

    .navbar-brand {
      font-weight: 800;
      font-size: 1.6rem;
      color: #fff !important;
    }
    
    .navbar-brand span {
      font-weight: 300;
    }
    
    .navbar-toggler {
        border-color: rgba(255, 255, 255, 0.3);
    }
    .navbar-toggler-icon {
        filter: invert(1);
    }

    /* --- Navigation Links --- */
    .nav-link {
      color: rgba(255, 255, 255, 0.8) !important;
      font-weight: 400;
      transition: color 0.3s, background-color 0.3s;
      margin: 0 5px; /* Added slight spacing */
      border-radius: 4px;
    }
    
    .nav-link:hover {
      color: #fff !important;
      background-color: rgba(255, 255, 255, 0.1);
    }
    
    /* --- Specific User Elements --- */
    .nav-link-user {
        background-color: var(--booknest-primary);
        color: #fff !important;
        font-weight: 600 !important;
        padding: 0.5rem 1rem !important;
        border-radius: 20px;
    }

    .nav-link-user:hover {
        background-color: #1a6f99;
        color: #fff !important;
    }
    
    .btn-login {
        background-color: var(--booknest-accent);
        color: #212529;
        font-weight: 700;
        border: none;
        transition: all 0.3s;
    }
    .btn-login:hover {
        background-color: #e0a800;
        transform: scale(1.05);
    }

  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark fixed-top shadow-sm">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <img src="https://book-nest-wgrp.onrender.com//src/logo.png" alt="BookNest Logo" height="30" class="me-2">
      Book<span>Nest</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarOverlay" aria-controls="navbarOverlay" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarOverlay">
      <ul class="navbar-nav ms-auto align-items-lg-center text-center text-lg-start">
        <% if (user) { %>
          <li class="nav-item me-lg-2 mb-2 mb-lg-0">
            <a class="nav-link nav-link-user" href="/users/account">
              <i class="bi bi-person-circle me-1"></i> Hello, <%= user.username %>
            </a>
          </li>
          
          <li class="nav-item"><a class="nav-link" href="/products"><i class="bi bi-book me-1"></i>Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/cart"><i class="bi bi-cart4 me-1"></i>Cart</a></li>
          <li class="nav-item"><a class="nav-link" href="/orders"><i class="bi bi-truck me-1"></i>Orders</a></li>
          
          <li class="nav-item ms-lg-2">
            <a class="nav-link text-danger fw-bold" href="/users/logout">
              <i class="bi bi-box-arrow-right me-1"></i> Logout
            </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a href="/users/login" class="btn btn-login px-3 py-2">
              <i class="bi bi-box-arrow-in-right me-1"></i> Log In / Sign Up
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>